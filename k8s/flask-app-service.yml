apiVersion: v1
kind: Service
metadata:
  name: flask-service 
  namespace: flux
spec:
  selector:
    app: flask-app
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 31000

